import Period from "./Period";
import { SUBJECTS } from "./subject";

const universalPeriods: Period[] = [
  new Period(3, 14, 20, 100, SUBJECTS.SPORTS, "Forsgrenska"),
];

export default universalPeriods;
